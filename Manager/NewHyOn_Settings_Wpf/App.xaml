<Application x:Class="NewHyOn.Settings.Wpf.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NewHyOn.Settings.Wpf">
    <Application.Resources>
        <Style x:Key="FlatButtonBase" TargetType="Button">
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D7DEE5" />
            <Setter Property="Foreground" Value="#20303F" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GhostActionButton" TargetType="Button" BasedOn="{StaticResource FlatButtonBase}">
            <Setter Property="Width" Value="84" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#D7DEE5" />
            <Setter Property="Foreground" Value="#20303F" />
        </Style>

        <Style x:Key="PrimaryActionButton" TargetType="Button" BasedOn="{StaticResource FlatButtonBase}">
            <Setter Property="Width" Value="104" />
            <Setter Property="Background" Value="#177E89" />
            <Setter Property="BorderBrush" Value="#177E89" />
            <Setter Property="Foreground" Value="#FFFFFF" />
        </Style>

        <Style x:Key="DialogPrimaryButton" TargetType="Button" BasedOn="{StaticResource FlatButtonBase}">
            <Setter Property="Width" Value="96" />
            <Setter Property="Background" Value="#177E89" />
            <Setter Property="BorderBrush" Value="#177E89" />
            <Setter Property="Foreground" Value="#FFFFFF" />
        </Style>

        <Style x:Key="DialogSecondaryButton" TargetType="Button" BasedOn="{StaticResource FlatButtonBase}">
            <Setter Property="Width" Value="132" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#D7DEE5" />
            <Setter Property="Foreground" Value="#20303F" />
        </Style>

        <Style x:Key="TrafficLightButtonBase" TargetType="Button">
            <Setter Property="Width" Value="14" />
            <Setter Property="Height" Value="14" />
            <Setter Property="Background" Value="#D1D5DB" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Focusable" Value="False" />
        </Style>

        <Style x:Key="TrafficCloseButton" TargetType="Button" BasedOn="{StaticResource TrafficLightButtonBase}">
            <Setter Property="Background" Value="#FF5F57" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                CornerRadius="7"
                                Background="{TemplateBinding Background}">
                            <Grid>
                                <Ellipse Margin="1"
                                         Fill="{TemplateBinding Background}" />
                                <Grid x:Name="IconLayer"
                                      Width="8"
                                      Height="8"
                                      Opacity="0">
                                    <Rectangle Width="8"
                                               Height="1.2"
                                               RadiusX="0.6"
                                               RadiusY="0.6"
                                               Fill="#5E5E5E"
                                               RenderTransformOrigin="0.5,0.5">
                                        <Rectangle.RenderTransform>
                                            <RotateTransform Angle="45" />
                                        </Rectangle.RenderTransform>
                                    </Rectangle>
                                    <Rectangle Width="8"
                                               Height="1.2"
                                               RadiusX="0.6"
                                               RadiusY="0.6"
                                               Fill="#5E5E5E"
                                               RenderTransformOrigin="0.5,0.5">
                                        <Rectangle.RenderTransform>
                                            <RotateTransform Angle="-45" />
                                        </Rectangle.RenderTransform>
                                    </Rectangle>
                                </Grid>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=StackPanel}}" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.9" />
                                <Setter TargetName="IconLayer" Property="Opacity" Value="1" />
                            </DataTrigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.76" />
                                <Setter TargetName="IconLayer" Property="Opacity" Value="1" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TrafficMinimizeButton" TargetType="Button" BasedOn="{StaticResource TrafficLightButtonBase}">
            <Setter Property="Background" Value="#FEBC2E" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                CornerRadius="7"
                                Background="{TemplateBinding Background}">
                            <Grid>
                                <Ellipse Margin="1"
                                         Fill="{TemplateBinding Background}" />
                                <Rectangle x:Name="IconLayer"
                                           Width="8"
                                           Height="1.2"
                                           RadiusX="0.6"
                                           RadiusY="0.6"
                                           Fill="#5E5E5E"
                                           Opacity="0" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=StackPanel}}" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.9" />
                                <Setter TargetName="IconLayer" Property="Opacity" Value="1" />
                            </DataTrigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.76" />
                                <Setter TargetName="IconLayer" Property="Opacity" Value="1" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TrafficZoomButton" TargetType="Button" BasedOn="{StaticResource TrafficLightButtonBase}">
            <Setter Property="Background" Value="#28C840" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                CornerRadius="7"
                                Background="{TemplateBinding Background}">
                            <Grid>
                                <Ellipse Margin="1"
                                         Fill="{TemplateBinding Background}" />
                                <Grid x:Name="IconLayer"
                                      Width="8"
                                      Height="8"
                                      Opacity="0">
                                    <Polygon x:Name="ZoomTrianglePrimary"
                                             Points="3.0,1.0 7.0,1.0 7.0,5.0"
                                             Fill="#5E5E5E"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center" />
                                    <Polygon Points="3.0,1.0 7.0,1.0 7.0,5.0"
                                             Fill="#5E5E5E"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"
                                             RenderTransformOrigin="0.5,0.5">
                                        <Polygon.RenderTransform>
                                            <TransformGroup>
                                                <ScaleTransform ScaleX="-1" ScaleY="-1" />
                                            </TransformGroup>
                                        </Polygon.RenderTransform>
                                    </Polygon>
                                </Grid>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=StackPanel}}" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.9" />
                                <Setter TargetName="IconLayer" Property="Opacity" Value="1" />
                            </DataTrigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.76" />
                                <Setter TargetName="IconLayer" Property="Opacity" Value="1" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CompactInputTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="40" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#43596B" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="CompactToggleCheckBox" TargetType="CheckBox">
            <Setter Property="Width" Value="44" />
            <Setter Property="Height" Value="24" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="Track"
                                Width="44"
                                Height="24"
                                Background="#CBD5E1"
                                CornerRadius="12"
                                Padding="2">
                            <Ellipse x:Name="Knob"
                                     Width="20"
                                     Height="20"
                                     Fill="#FFFFFF"
                                     HorizontalAlignment="Left" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#177E89" />
                                <Setter TargetName="Knob" Property="HorizontalAlignment" Value="Right" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
